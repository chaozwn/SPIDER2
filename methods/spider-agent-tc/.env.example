# Copy to `.env` and fill in. `run.smoke.sh` will auto-source it.
# DO NOT commit the real .env file.

OPENAI_API_KEY=sk-xxxx
OPENAI_API_BASE=https://api.openai.com/v1

# Optional: override model from CLI/env if you want to switch quickly
MODEL=gpt-5.5
